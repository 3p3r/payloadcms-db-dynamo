# Copy to .env — Payload 3.x kitchen sink (DynamoDB Local via repo-root docker compose)

PAYLOAD_SECRET=kitchen-sink-dev-secret
PORT=3000

DYNAMODB_TABLE_NAME=payload-kitchen-sink-3
DYNAMODB_ENDPOINT=http://localhost:8000
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
