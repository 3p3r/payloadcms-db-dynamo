services:
  dynamodb-local:
    image: amazon/dynamodb-local:3.0.0
    container_name: payloadcms-db-dynamo-test
    ports:
      - "${PAYLOAD_DDB_TEST_PORT:-8000}:8000"
    command: -jar DynamoDBLocal.jar -inMemory -sharedDb
